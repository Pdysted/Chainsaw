<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.philip.chainsaw.MainActivity">

    <LinearLayout
        android:orientation="horizontal"
        android:id="@+id/headerbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layoutDirection="rtl"
        android:background="@color/colorPrimary">
        <ImageButton
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@mipmap/chat_menu"
            android:background="#01000000"
            android:onClick="goToMessages"/>
        <ImageButton
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginRight="110dp"
            android:src="@mipmap/recs_icon"
            android:background="#01000000"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/ss"
        android:orientation="vertical"
        android:layout_marginLeft="10pt"
        android:layout_below="@id/headerbar"
        android:layout_width="280dp"
        android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/userPicView"

        android:layout_width="280dp"
        android:layout_height="256dp"
        android:layout_marginBottom="-4dp">
        </ImageView>

    <TextView
        android:id="@+id/userInfoView"
        android:background="@drawable/rounded_corner_user_info"
        android:textColor="#000000"
        android:layout_below="@id/userPicView"
        android:layout_alignBaseline="@id/userPicView"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="14dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp">
       </TextView>
    </LinearLayout>

</RelativeLayout>
